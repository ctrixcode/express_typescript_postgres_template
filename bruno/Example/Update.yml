info:
  name: "Update "
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseURL}}/api/examples/uuid"
  body:
    type: json
    data: |-
      {
        "name": "Updated Example Name",
        "price": 59
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
