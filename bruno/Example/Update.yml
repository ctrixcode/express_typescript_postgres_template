info:
  name: "Update "
  type: http
  seq: 6

http:
  method: PUT
  url: "{{baseURL}}/api/examples/1"
  body:
    type: json
    data: |-
      {
        "name": "Updated Example Name",
        "price": 59
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
