info:
  name: Get By Category
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseURL}}/api/examples/category/electronics"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
